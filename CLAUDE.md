# このリポジトリで作業するときの決めごと

## 🕐 時刻は必ずコマンドで確認する（最優先。例外なし）

**このコンテナの時計は UTC。本人は日本にいる。JST = UTC+9。**

### 発言する前に必ず実行する

```bash
TZ=Asia/Tokyo date '+JST %Y-%m-%d %H:%M (%a)'
```

**次のいずれかを書く前は、毎回これを実行する。**

| 書こうとしているもの | 例 |
|---|---|
| 現在時刻 | 「いま15時です」 |
| 日付・曜日 | 「今日は8/14(金)です」 |
| **残り時間** | **「あと2時間で面談です」** |
| 相対表現 | 「今日」「明日」「今夜」「今週」 |
| 締切までの日数 | 「締切まで5日」 |

### 禁止

- **会話の流れから時刻を推測して書く。** これで2回間違えている（2026-08-11、2026-08-14）
- **UTC の値に頭の中で9を足す。** 計算せず、コマンドの出力をそのまま使う
- コマンドを実行していない状態で、時刻に言及する

### 実行していない／できない場合

**時刻に言及しない。** 「あと少しで」「まもなく」も使わない。
必要なら「時刻を確認します」と言ってコマンドを実行する。

> **2026-08-14 本人指摘により追加。**
> 「約束」ではなく「毎回コマンドを打つ」という手順にする。
> 覚えていることに頼らない。

---

## 📅 カレンダーは angelswingsinvest@gmail.com

**Gmail は flying.knight333、カレンダーとドライブは angelswingsinvest。**
これは意図した状態（PLAN 3.5 の「接続アカウントの取り決め・B案」）。不具合ではない。

- **予定の正は angelswingsinvest のカレンダー。** 面談はここに入れる
- flying.knight333 側のカレンダーは Claude から見えない

**面談枠として提示できるのは、月・火・水・木の 10:00–13:00 と 14:00–17:00。**
**金曜は HashPort 出社で終日NG（2026年9月末まで）。**

> **2026-09-04 本人指摘により変更。出社日は水曜 → 金曜。**
> エージェントに伝える「終日NGの曜日」も**金曜**に読み替える。

---

## ✉️ メールの書式

**① 宛名は「社名 → 個人名」の順。社名を先頭に置く**
**② 署名も「社名 → 個人名」の順**

```
株式会社◯◯◯◯
◯◯様

お世話になっております。
合同会社櫻源郷の松田徳義です。

（本文）

引き続きよろしくお願いいたします。

──────────────────
合同会社櫻源郷
松田 徳義（まつだ のりよし）
Mail: flying.knight333@gmail.com
──────────────────
```

**メールの送信はしない。下書きの作成まで。** 送信ボタンは本人が押す。

---

## 📝 応募文の鉄則

- **否定文を書かない。** 「〜の経験はありません」「必須要件を満たしていないことは承知の上で」は**絶対に書かない**
- 満たせない要件には**触れない**。聞かれてから答える
- **応募したら、その場で本文を `APPLICATION_DRAFTS.md` に残す。** 残っていない応募は落ちても検証できない
- **必須に「コンサルティングファーム」が明記された案件は出さない**（2026-08-13 学習。書き方を工夫しても通らない）。ただし先方から声がかかった場合は別

---

## 📊 進捗の正は Google スプレッドシート

**01_応募一覧**
https://docs.google.com/spreadsheets/d/16lTbCJysl0PdK2zWgAoBv_E3mZn__Wvewez4dHRcoG0/edit

サービスアカウント経由で Claude が直接読み書きできる（鍵はリポジトリ外・`.gitignore` で保護）。

```python
from google.oauth2 import service_account
from googleapiclient.discovery import build
S='/tmp/claude-0/-home-user-test/4b2dd49b-bb28-53b0-8d40-8b0cf78a00da/scratchpad/secrets/sa.json'
SHEET='16lTbCJysl0PdK2zWgAoBv_E3mZn__Wvewez4dHRcoG0'
c=service_account.Credentials.from_service_account_file(S,scopes=['https://www.googleapis.com/auth/spreadsheets'])
svc=build('sheets','v4',credentials=c,cache_discovery=False)
```

---

## 🗣 本人とのやりとり

- **敬語を使う**
- **二人称に「君」を使わない**
- **本人が決める。こちらは実行する。**方針が決まっている論点を蒸し返さない
- 予定が変わったら **その場で PLAN.md を直す**

---

## 📁 ファイルの役割

| ファイル | 中身 |
|---|---|
| `PLAN.md` | **戦略・タスク・スケジュールの1枚。**迷ったらここ |
| `HANDOFF.md` | 背景・経緯・資産の所在 |
| `APPLICATION_DRAFTS.md` | 提出した応募文の全文 |
| `MAIL_WATCH.md` | メール監視ログ |
| `INTERVIEW_*.md` | 面談ごとの想定問答 |
| `MEDIA_SURVEY_*.md` | 媒体ごとの実測 |
